server:
  port: 8080

spring:
  datasource:
    url: jdbc:postgresql://postgres-db:5432/smart-home
    username: postgres
    password: secret

kafka:
  bootstrap-servers: kafka:29092

eureka:
  client:
    service-url:
      defaultZone: http://discovery-server:8761/eureka/
